services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      livekit:
        condition: service_started
      minio:
        condition: service_started
    labels:
      - traefik.enable=true
      - traefik.http.routers.mediconnect.rule=Host(`mediconnect.aliahad.com`)
      - traefik.http.routers.mediconnect.entrypoints=websecure
      - traefik.http.routers.mediconnect.tls.certresolver=letsencrypt
      - traefik.http.services.mediconnect.loadbalancer.server.port=3000
    networks:
      - internal
      - traefik-public
    restart: unless-stopped

  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrate
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mediconnect
      POSTGRES_PASSWORD: mediconnect
      POSTGRES_DB: mediconnect
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mediconnect -d mediconnect"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - internal
    restart: unless-stopped

  livekit:
    image: livekit/livekit-server:latest
    entrypoint: /bin/sh
    command:
      - -c
      - |
        cat > /tmp/livekit.yaml <<EOF
        port: 7880
        rtc:
          tcp_port: 7881
          udp_port: 7882
          use_external_ip: true
        keys:
          devkey: aVeryLongSecretKeyThatIsAtLeast32Characters
        EOF
        exec livekit-server --config /tmp/livekit.yaml --node-ip 72.62.162.106
    labels:
      - traefik.enable=true
      # HTTP router for LiveKit signaling
      - traefik.http.routers.livekit.rule=Host(`livekit.aliahad.com`)
      - traefik.http.routers.livekit.entrypoints=websecure
      - traefik.http.routers.livekit.tls.certresolver=letsencrypt
      - traefik.http.services.livekit.loadbalancer.server.port=7880
      # CORS headers
      - traefik.http.middlewares.livekit-cors.headers.accesscontrolallowmethods=GET,POST,PUT,DELETE,OPTIONS
      - traefik.http.middlewares.livekit-cors.headers.accesscontrolallowheaders=*
      - traefik.http.middlewares.livekit-cors.headers.accesscontrolalloworiginlist=https://mediconnect.aliahad.com
      - traefik.http.middlewares.livekit-cors.headers.accesscontrolmaxage=100
      - traefik.http.middlewares.livekit-cors.headers.addvaryheader=true
      - traefik.http.routers.livekit.middlewares=livekit-cors
    ports:
      - "7881:7881"
      - "7882:7882/udp"
    networks:
      - internal
      - traefik-public
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    networks:
      - internal
    restart: unless-stopped

volumes:
  postgres_data:
  minio_data:

networks:
  internal:
    driver: bridge
  traefik-public:
    external: true
